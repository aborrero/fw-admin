#!/bin/bash

$IPSET create $REPOS hash:ip family $INET -exist
$IPSET add $REPOS $DL_DROPBOX_COM -exist
$IPSET add $REPOS $DOWNLOAD_FEDORA_PROJECT_ORG -exist
$IPSET add $REPOS $CENTOS_CICT_FR -exist
$IPSET add $REPOS $CENTOS_COPAHOST_COM -exist
$IPSET add $REPOS $CISNE_SIM_UCM_ES -exist
$IPSET add $REPOS $CLAMAV_N2000_NET -exist
$IPSET add $REPOS $CLOUD_GITHUB_COM -exist
$IPSET add $REPOS $CNS_SERVICES_TMP_SUN_COM -exist
$IPSET add $REPOS $CONTENT_SATELLITE_RHN_REDHAT_COM -exist
$IPSET add $REPOS $CONTENT_WEB_RHN_REDHAT_COM -exist
$IPSET add $REPOS $CONTENT_XMLRPC_RHN_REDHAT_COM -exist

