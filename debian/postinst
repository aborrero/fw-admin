#!/bin/bash

set -e

# Add service
update-rc.d fw defaults >/dev/null

if [ "$1" == "configure" ] && [ -z "$2" ]
then
	# Only start service if is a new installation, no update.
	invoke-rc.d fw start

	# Do a basic installation of datafiles
	fw-admin --check-datafiles --install
fi

exit 0
